<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Proyecto_2_Movil.Views.SeleccionRazaPage"
             Title="{Binding Titulo}">

    <ScrollView Padding="20">
        <VerticalStackLayout Spacing="16">
            <Label Text="{Binding Titulo}"
                   FontSize="22"
                   HorizontalOptions="Center"
                   FontAttributes="Bold" />

            <CollectionView ItemsSource="{Binding Razas}" SelectionMode="None">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame Margin="0,6" Padding="16" CornerRadius="12" BorderColor="LightGray">
                            <VerticalStackLayout Spacing="8">
                                <Label Text="{Binding Nombre}" FontSize="18" FontAttributes="Bold" />
                                <Label Text="{Binding Descripcion}" FontSize="14" TextColor="Gray" />
                                <Button Text="Seleccionar"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.SeleccionarRazaCommand}"
                                        CommandParameter="{Binding .}"
                                        BackgroundColor="#6750A4"
                                        TextColor="White"
                                        CornerRadius="8" />
                            </VerticalStackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>