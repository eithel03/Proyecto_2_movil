<?xml version="1.0" encoding="utf-8" ?>
<ContentPage 
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:Proyecto_2_Movil.ViewModels"
    x:Class="Proyecto_2_Movil.Views.SeleccionArmaPage">

    <ContentPage.BindingContext>
        <vm:SeleccionArmaViewModel />
    </ContentPage.BindingContext>

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="15">
            <Label Text="{Binding Titulo}" FontSize="24" HorizontalOptions="Center" />

            <CollectionView ItemsSource="{Binding Armas}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame Padding="10" Margin="5" BorderColor="Gray">
                            <VerticalStackLayout>
                                <Label Text="{Binding Nombre}" FontSize="20" />
                                <Label Text="{Binding Descripcion}" FontSize="14" TextColor="Gray"/>

                                <Button Text="Seleccionar"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type vm:SeleccionArmaViewModel}}, Path=SeleccionarArmaCommand}"
                                        CommandParameter="{Binding}" />
                            </VerticalStackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
