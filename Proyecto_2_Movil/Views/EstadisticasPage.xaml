<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Proyecto_2_Movil.ViewModels"
             x:Class="Proyecto_2_Movil.Views.EstadisticasPage"
             Title="Estadísticas">

    <VerticalStackLayout Padding="20" Spacing="12">

        <Label Text="Estadísticas de jugadores"
               FontSize="20"
               HorizontalOptions="Center"
               Margin="0,0,0,10" />

        <CollectionView ItemsSource="{Binding Jugadores}">
            <CollectionView.ItemTemplate>
                <!-- Bindings NO compilados, funcionan en runtime sin problema -->
                <DataTemplate>
                    <Frame Padding="10" Margin="0,0,0,8" HasShadow="False" BorderColor="#444">
                        <VerticalStackLayout Spacing="4">
                            <Label Text="{Binding Nombre}"
                                   FontAttributes="Bold"
                                   FontSize="16" />

                            <Label Text="{Binding Jugadas, StringFormat='Partidas jugadas: {0}'}" />
                            <Label Text="{Binding Ganadas, StringFormat='Ganadas: {0}'}" />
                            <Label Text="{Binding Perdidas, StringFormat='Perdidas: {0}'}" />
                            <Label Text="{Binding Empates, StringFormat='Empates: {0}'}" />
                            <Label Text="{Binding PorcentajeVictoria, StringFormat='Winrate: {0:F1}%'}" />
                        </VerticalStackLayout>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

    </VerticalStackLayout>
</ContentPage>
